[access]
	inheritFrom = All-Projects
[access "refs/*"]
	owner = group user/MicroScopes (microscopes)
[access "refs/for/refs/*"]
	read = group user/MicroScopes (microscopes)
	push = group user/MicroScopes (microscopes)
	label-Code-Review = -2..+2 group user/MicroScopes (microscopes)
	label-Verified = -1..+1 group user/MicroScopes (microscopes)
	rebase = group user/MicroScopes (microscopes)
	removeReviewer = group user/MicroScopes (microscopes)
	submit = group user/MicroScopes (microscopes)
[access "refs/heads/*"]
	read = group user/MicroScopes (microscopes)
	create = group user/MicroScopes (microscopes)
	push = +force group user/MicroScopes (microscopes)
	pushMerge = group user/MicroScopes (microscopes)
[access "refs/tags/*"]
	push = +force group user/MicroScopes (microscopes)
	pushTag = group user/MicroScopes (microscopes)
	pushSignedTag = group user/MicroScopes (microscopes)
